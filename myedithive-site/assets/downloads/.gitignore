# macOS app installers are 400MB+ — exceed GitHub's 100MB limit.
# They are distributed via Netlify, not git. Plugin pkgs (small) are tracked.
# Patterns cover the tokened filenames too.
edithive-select-mac-*.pkg
edithive-select-windows*.exe
